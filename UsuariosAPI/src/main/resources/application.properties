# Nombre de la aplicaci�n
spring.application.name=UsuariosAPI

# Asignamos un puerto nuevo para que no choque con la otra API
server.port=8082

# Configuraci�n de la Base de Datos MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/usuarios_api?serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=

# Configuraci�n de JPA/Hibernate
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update

# Propiedades para depuraci�n
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true